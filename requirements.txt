httpx>=0.27
python-dotenv>=1.0
tzdata>=2024.1
aiogram>=3.4.0
# Транскрибация голосовых. fastapi-poe - лёгкий фолбэк на существующем POE_API_KEY.
# Локальный Whisper (быстрее и без лимитов) ставится отдельно:
#   pip install faster-whisper
fastapi-poe>=0.0.36
